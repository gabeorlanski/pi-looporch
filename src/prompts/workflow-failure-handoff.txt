<workflow_handoff event="failed">
Call `resume_workflow` with run ID `{{runId}}` to replay unchanged completed model calls and continue from the first changed or incomplete call.

Workflow: `{{workflowName}}`
Run ID: `{{runId}}`

Failure:

{{failure}}
</workflow_handoff>
