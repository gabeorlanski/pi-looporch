Handle this workflow request in the current session.

If an existing workflow fits, call run_workflow with its name and JSON input.

If required input for an existing workflow is missing or ambiguous, ask the user a concise clarification question before calling tools.

If a new reusable workflow is clearly needed, first call workflow_primitives({ primitive: "authoring" }), then request only specific primitive docs as needed. Draft the smallest budget-aware workflow and propose it with propose_workflow. Use debug_workflow only for small deterministic checks with fake agentResponses.

Available workflows: {{availableWorkflowNames}}

Request:

{{request}}
