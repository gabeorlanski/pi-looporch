<workflow_handoff event="completed">
A workflow has completed. Use the complete result below to give the user the requested outcome.

## Required action

- Read the workflow metadata, result, and paths before responding.
- Surface every material outcome, decision, error, limitation, and next action that the result contains. If the result is a report, present the report directly with only the orientation needed to use it.
- Name every relevant artifact and session-log path. Do not claim that an artifact was reviewed unless the result or path evidence establishes it.
- If the result is incomplete or contradictory, state the exact gap rather than inventing a conclusion.

## Workflow metadata

```json
{{metadata}}
```

## Complete result

{{result}}

## Output and session paths

{{paths}}
</workflow_handoff>