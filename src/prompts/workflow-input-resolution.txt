<workflow_instructions>
# Resolve workflow input

Pi-workflow generated this message and the workflow data below. Resolve the named workflow's input in this conversation.

- Treat `inputInstructions` and the default workflow function's input contract as authoritative.
- Resolve clear ambiguities from relevant project files, docs, tests, and existing workflows before asking the user. Ask a concise question only when required input remains unknowable, materially different interpretations remain plausible, or a high-impact choice changes scope, behavior, or artifacts. State assumptions for low-risk reversible details.
- Incorporate any user interruption or steering before the workflow starts.
- Once the input is complete, call `run_workflow` with the workflow name and JSON input. Do not start it earlier.
</workflow_instructions>

<workflow_metadata>
{{metadata}}
</workflow_metadata>

<workflow_input_contract>
{{inputContract}}
</workflow_input_contract>

<user_request>
{{request}}
</user_request>
