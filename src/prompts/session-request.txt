<workflow_instructions>
# Workflow request

Pi-workflow generated this message for the current session. The user request is the only user-authored instruction and appears below.

- First decide whether a named workflow completes the requested outcome. If so, resolve its complete input and call `run_workflow`.
- Resolve available facts from project files, docs, tests, and workflows. Ask only for unknowable inputs, materially different interpretations, or high-impact choices; state low-risk reversible assumptions.
- To author a reusable workflow, call `workflow_design_guidance({ topic: "overview" })`. Load only the additional guidance topics used by that workflow, including `prompt-files` before writing a child prompt.
- Create the complete draft at `<draft-root>/<workflow-name>/`, including every workflow-owned resource, then call `propose_workflow`. Omit `draftDir` only for the default root below.
</workflow_instructions>

<workflow_draft_root>
{{defaultWorkflowDraftRoot}}
</workflow_draft_root>

<available_workflows>
{{availableWorkflowNames}}
</available_workflows>

<user_request>
{{request}}
</user_request>
