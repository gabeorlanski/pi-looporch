# Structured outputs

- Use `agent({ template, values }, { schema })` when a child must return structured fields.
- Use `LLM(prompt, { schema })` when structured data needs one generation without tools or a child session. It decodes and validates exactly one response with no repair call.
- `LLM` always returns `text`, `output`, `usage`, `model`, `provider`, and `stopReason`. Raw calls use `output: null`; schema calls place the validated JSON value in `output`.
- The schema is prepended to the child prompt in a `structured_output_schema` section and becomes the terminal `StructuredOutput` tool parameter contract. Describe each field's meaning inline with JSON Schema descriptions.
- `StructuredOutput` validates keyword arguments, captures them, and ends the child session. A schema-enabled child that does not call it fails.
- The agent result always includes `message`, `name`, `steps`, and `usage`. `message` is `null` when a terminal call omits it; `name`, `steps`, and `usage` are runtime-owned and cannot be schema properties.
- Keep schemas shallow and semantic: required fields, useful descriptions, enums/booleans, and `additionalProperties: false`. Add bounds only for real downstream limits.
- Use JSON as the control surface only: status, decision, IDs, counts, short summary, artifact paths, evidence paths, and line references.
- Route large reasoning, transcripts, diffs, reports, and generated artifacts to named files; return their paths and a compact manifest.
- For large lists, prefer capped summaries or JSONL/artifact files over one giant nested JSON object.
- Stage expansion: first return a compact selector/manifest, then launch follow-up agents only for selected items that need detail.
- Use stable IDs and lookup tables instead of repeating long strings across every finding.

```js
const schema = {
  type: "object",
  properties: { status: { type: "string", enum: ["pass", "fail"] }, summary: { type: "string" } },
  required: ["status", "summary"],
  additionalProperties: false,
};
```
