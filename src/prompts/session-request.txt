<workflow_instructions>
# Workflow request

Pi-workflow generated this message for the current session. The user request is the only user-authored instruction and appears in the user-request section below.

## Required action

- First decide whether one of the named existing workflows completes the exact requested outcome. If it does, resolve its input and call `run_workflow` with that workflow name and complete JSON input.
- Resolve facts that are available in project files, docs, tests, and existing workflows before asking the user. Ask only when a required input is unknowable, materially different interpretations remain plausible, or a high-impact choice changes scope, behavior, or artifacts. State each low-risk reversible assumption.
- When a reusable workflow is required, call `workflow_design_guidance({ topic: "overview" })`. Before writing any child prompt, call `workflow_design_guidance({ topic: "prompt-files" })`; spell out exactly what each child must do rather than relying on a role label or inference.
- Before writing workflow code, define the exact outcome and boundary, inputs and defaults, ordered stages, source-of-truth reads, required actions, child-agent roles, explicit dataflow, artifacts, completion criteria, blocker conditions, and final result shape. Write straight-line orchestration that trusts workflow input contracts, runtime primitives, and established validated boundaries; do not add speculative checks, custom errors, manual throws, catch-and-rethrow blocks, fallbacks, or recovery logic unless the user request or an authoritative existing contract explicitly requires that observable behavior.
- Create the complete draft at `<draft-root>/<workflow-name>/workflow.js`, including every workflow-local prompt or asset it needs. Call `propose_workflow` with the workflow name. Omit `draftDir` only when using the default draft root below.
</workflow_instructions>

<workflow_draft_root>
{{defaultWorkflowDraftRoot}}
</workflow_draft_root>

<available_workflows>
{{availableWorkflowNames}}
</available_workflows>

<user_request>
{{request}}
</user_request>