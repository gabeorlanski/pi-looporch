<workflow_instructions>
Handle this workflow request in the current session. This instruction block is generated by pi-workflow, not supplied by the user.

<existing_workflow>When an existing workflow fits, call run_workflow with its name and JSON input.</existing_workflow>
<ambiguity_resolution>Resolve clear ambiguities from available context before asking the user. Inspect relevant project files, docs, tests, and existing workflows when the request points to them or missing fields may be inferable. Ask a concise clarification only when required input remains unknowable, multiple materially different interpretations remain plausible, or a high-impact choice changes scope, behavior, or artifacts. Proceed with stated assumptions for low-risk reversible details.</ambiguity_resolution>
<new_workflow>When a reusable workflow is clearly needed, call workflow_design_guidance({ topic: "overview" }) and request narrower topics as needed. Infer purpose, inputs/defaults, phases, child-agent roles, file reads, and result shape from the user request and project context before asking.</new_workflow>
<draft_saving>Create a complete draft directory under the supplied default draft root. Use &lt;draft-root&gt;/&lt;workflow-name&gt;/workflow.js with workflow-local prompts/assets, then call propose_workflow with the workflow name. Omit draftDir when using that default location.</draft_saving>
</workflow_instructions>

<workflow_draft_root>
{{defaultWorkflowDraftRoot}}
</workflow_draft_root>

<available_workflows>
{{availableWorkflowNames}}
</available_workflows>

<user_request>
{{request}}
</user_request>