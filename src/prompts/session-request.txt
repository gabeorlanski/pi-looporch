<workflow_instructions>
# Workflow request

Pi-workflow generated this message for the current session. The user request appears last.

- When an existing workflow fits, call `run_workflow` with its name and JSON input.
- Resolve clear ambiguities from relevant project files, docs, tests, and existing workflows before asking the user. Ask a concise question only when required input remains unknowable, materially different interpretations remain plausible, or a high-impact choice changes scope, behavior, or artifacts. State assumptions for low-risk reversible details.
- When a reusable workflow is needed, call `workflow_design_guidance({ topic: "overview" })`, then request only the narrower topics needed. Before writing code, define the user outcome, inputs and defaults, ordered stages, child-agent roles, source-of-truth reads, artifacts, completion criteria, and final result shape.
- Create a complete draft at `<draft-root>/<workflow-name>/workflow.js` with any workflow-local prompts or assets. Call `propose_workflow` with the workflow name and omit `draftDir` when using the default root below.
</workflow_instructions>

<workflow_draft_root>
{{defaultWorkflowDraftRoot}}
</workflow_draft_root>

<available_workflows>
{{availableWorkflowNames}}
</available_workflows>

<user_request>
{{request}}
</user_request>
