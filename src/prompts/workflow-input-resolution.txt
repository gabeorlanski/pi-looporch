<workflow_instructions>
# Resolve workflow input

Pi-workflow generated this message. The workflow metadata, input contract, and user request below define the task.

## Required action

- Treat `inputInstructions` and the default workflow function's input contract as authoritative. Produce every required input field in the type and shape the contract requires.
- Resolve each fact that project files, docs, tests, existing workflows, or the user request make clear. Do not ask the user to repeat information that those sources establish.
- Ask the user only when a required value is unknowable, materially different interpretations remain plausible, or a high-impact choice changes scope, behavior, or artifacts. Name the exact missing value, why it matters, and the available choices when known.
- State every low-risk reversible assumption and incorporate any user interruption or steering received before the workflow starts.
- Call `run_workflow` only after the input is complete. Pass the named workflow and the complete JSON input; do not start the workflow with partial, invented, or unsupported required values. Context-supported resolution is required.
</workflow_instructions>

<workflow_metadata>
{{metadata}}
</workflow_metadata>

<workflow_input_contract>
{{inputContract}}
</workflow_input_contract>

<user_request>
{{request}}
</user_request>